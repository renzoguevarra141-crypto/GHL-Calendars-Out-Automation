# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# GHL (GoHighLevel) API
# API key needs calendars.readonly + calendars.write scopes
GHL_API_KEY=
GHL_LOCATION_ID=
